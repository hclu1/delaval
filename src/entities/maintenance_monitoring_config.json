{
  "name": "maintenance_monitoring_config",
  "type": "object",
  "required": [
    "key",
    "value",
    "updatedAt"
  ],
  "properties": {
    "key": {
      "bsonType": "string",
      "description": "Clé de configuration (green_threshold, orange_threshold, red_threshold, email_enabled, etc.)"
    },
    "value": {
      "bsonType": "string",
      "description": "Valeur de configuration en JSON"
    },
    "description": {
      "bsonType": "string",
      "description": "Description de la configuration"
    },
    "creator": {
      "bsonType": "string",
      "description": "Creator user ID"
    },
    "creatorEmail": {
      "bsonType": "string",
      "description": "Creator user email"
    },
    "createdAt": {
      "bsonType": "string",
      "title": "datetime",
      "description": "ISO 8601 datetime string (UTC): YYYY-MM-DDTHH:mm:ss.sssZ"
    },
    "updatedAt": {
      "bsonType": "string",
      "title": "datetime",
      "description": "ISO 8601 datetime string (UTC): YYYY-MM-DDTHH:mm:ss.sssZ"
    }
  }
}